/**
 * USYC NAV conversion seam — Balance Sheet panel (Panel A).
 *
 * spec: docs/frontend/dashboard-components.md#usycnav (seam design, v1 stub behavior).
 */

/**
 * Converts a USYC amount to its USDC equivalent. v1: 1:1 identity stub — see
 * spec: docs/frontend/dashboard-components.md#usycnav.
 */
export function convertUsycToUsdc(usycAmount: bigint): bigint {
  return usycAmount;
}

# Frontend hooks

Catalogue of reused React hooks. Governed by [`docs/FRONTEND.md` → Code structure rules](../FRONTEND.md#code-structure-rules).

**Inclusion criteria.** A hook appears here when it is consumed by two or more components, or is explicitly designed for reuse (e.g. shipped from `@pipeline/ui` or the wallet module). Component-local hooks following the "view + co-located hook" rule (e.g. `useStakeCard` next to `StakeCard.tsx`) are intentionally **excluded** — they have one owner and one call site.

Entries are sorted alphabetically by name.

| Name                            | Import path   | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `isMockKeyPresent`              | `@/wallet`    | Non-reactive helper: returns `true` when a `pipeline.mock.wallet.*` key is currently set in `localStorage`. Used by the `/test` diagnostic page to render `MOCKED` badges; see [`/test` route](../../packages/frontend/src/routes/test.tsx) for usage.                                                                                                                                                                                                                                                                                                                          |
| `useApproval`                   | `@/wallet`    | Reads ERC-20 `allowance(owner, spender)` and exposes `approve(spender, amount)` for any (token, spender) pair. Returns `{ allowance, isSufficient, approve, data, isLoading, isPending, isSuccess, error, reset, refetch }`. Honours `pipeline.mock.wallet.allowance.<token>.<spender>` and `pipeline.mock.wallet.contract.<token>.approve` for mock testing.                                                                                                                                                                                                                   |
| `useClaim`                      | `@/wallet`    | Write hook for `claim(requestId, verifierSignature)` on the DepositManager contract. Returns `{ write, data, isPending, isSuccess, error, reset }`. Honours `pipeline.mock.wallet.contract.depositManager.claim` for mock testing.                                                                                                                                                                                                                                                                                                                                              |
| `useClaimWithdrawal`            | `@/wallet`    | Write hook for `claimWithdrawal(requestId, verifierSignature)` on the WithdrawalQueue contract. Returns `{ write, data, isPending, isSuccess, error, reset }`. Honours `pipeline.mock.wallet.contract.withdrawalQueue.claimWithdrawal` for mock testing.                                                                                                                                                                                                                                                                                                                        |
| `useContractRead`               | `@/wallet`    | Generic read hook wrapping wagmi's `useReadContract` with the per-address localStorage mock layer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `useDepositManagerAddresses`    | `@/wallet`    | Reads the `plUsd()` and `usdc()` addresses from the DepositManager contract. Returns `{ plusd, usdc, isLoading, error }` with "fetch once" caching. Honours `pipeline.mock.wallet.contract.depositManager.plusd/usdc` named aliases.                                                                                                                                                                                                                                                                                                                                            |
| `useDepositManagerMinDeposit`   | `@/wallet`    | Reads the `minDeposit()` view from the DepositManager contract. Returns `{ minDeposit: bigint \| undefined, isLoading, error }` with "fetch once" caching. Honours `pipeline.mock.wallet.contract.depositManager.minDeposit` (named alias, takes priority) and `pipeline.mock.wallet.contract.<address>.minDeposit` (generic per-address fallback).                                                                                                                                                                                                                             |
| `useDepositVoucher`             | `@/api`       | React Query hook that fetches the deposit voucher (verifier signature) from `GET /v1/deposits/{request_id}/voucher?wallet=<addr>`. Returns `{ data, status, error, refetch }` where `status` is `"idle" \| "pending" \| "ready" \| "failed"`. Disabled when `requestId` is `undefined` or the wallet is disconnected. Polls every 3 s; retries up to 20 times on retriable errors. Honours `pipeline.mock.api.GET./v1/deposits/<requestId>/voucher` mock key.                                                                                                                   |
| `useRequestDeposit`             | `@/wallet`    | Write hook for `requestDeposit(amount)` on the DepositManager contract. Returns `{ write, data, isPending, isSuccess, error, reset }`. Honours `pipeline.mock.wallet.contract.depositManager.requestDeposit` for mock testing.                                                                                                                                                                                                                                                                                                                                                  |
| `useRequestWithdrawal`          | `@/wallet`    | Write hook for `requestWithdrawal(amount)` on the WithdrawalQueue contract. Returns `{ write, data: { hash, requestId?, queued? }, isPending, isSuccess, error, reset }`. Honours `pipeline.mock.wallet.contract.withdrawalQueue.requestWithdrawal` for mock testing; `requestId` and `queued` are mock-path only.                                                                                                                                                                                                                                                              |
| `useStake`                      | `@/wallet`    | Write hook for `deposit(assets, receiver)` on the StakedPLUSD ERC-4626 vault; `receiver` defaults to the connected wallet. Returns `{ write, data: { hash, shares? }, isPending, isSuccess, error, reset }`. Honours `pipeline.mock.wallet.contract.stakedPlusd.stake` for mock testing; `shares` is mock-path only.                                                                                                                                                                                                                                                            |
| `useStakedPlusdAsset`           | `@/wallet`    | Reads `asset()` from the StakedPLUSD vault — returns the underlying PLUSD token address. Returns `{ plusd, isLoading, error }` with "fetch once" (`staleTime: Infinity`) caching. Honours `pipeline.mock.wallet.contract.stakedPlusd.asset` named alias and `pipeline.mock.wallet.contract.<address>.asset` generic per-address fallback.                                                                                                                                                                                                                                       |
| `useStakedPlusdConvertToAssets` | `@/wallet`    | Reads `convertToAssets(shares)` from the StakedPLUSD vault — "how much PLUSD do I get for this many sPLUSD?" Returns `{ data: bigint \| undefined, isLoading, error }`. Disabled when input is `undefined` or `0n`. Honours rate-scalar mock at `pipeline.mock.wallet.contract.stakedPlusd.convertToAssets` (output = `(shares * rate) / 1e18`).                                                                                                                                                                                                                                |
| `useStakedPlusdConvertToShares` | `@/wallet`    | Reads `convertToShares(assets)` from the StakedPLUSD vault — "how many sPLUSD shares do I get for this much PLUSD?" Returns `{ data: bigint \| undefined, isLoading, error }`. Disabled when input is `undefined` or `0n`. Honours rate-scalar mock at `pipeline.mock.wallet.contract.stakedPlusd.convertToShares` (output = `(assets * rate) / 1e18`).                                                                                                                                                                                                                         |
| `useStellarWallet`              | `@/wallet`    | Returns the connected Stellar wallet's address, connection state, and `connect`/`disconnect` actions. Backed by `@creit.tech/stellar-wallets-kit` v2.x singleton SDK; honours `pipeline.mock.wallet.stellar.*` localStorage keys. `connect()` is gated by the shared chain-agnostic first-connection terms modal (same `FirstConnectionModal` instance as EVM) — the kit auth modal only opens after the user acknowledges. Mock-wallet path bypasses the gate as a dev affordance. Balance not yet wired (sub-issue 2).                                                                                                                                                                                                                                                       |
| `useTermsAcknowledgement`       | `@/wallet`    | Reactive, chain-agnostic hook that tracks whether the user has acknowledged the jurisdiction self-attestation gate (once for all chains — EVM and Stellar). Returns `{ acknowledged: boolean, acknowledge() }`. Reads and writes the single flat key `pipeline.wallet.termsAcknowledged` in localStorage; syncs across tabs via the native `storage` event. A non-hook companion `readTermsAcknowledged()` (argument-less) is provided for synchronous gate checks inside click handlers. Migrates legacy address-scoped / pending keys on first read so existing users are never re-prompted.                                                                                                                                                                                        |
| `useEvmToken`                   | `@/wallet`    | Bundles ERC-20 metadata (`decimals`, `symbol`), balance (`balanceOf`), and approval (`useApproval` composition) into one return value for the connected wallet. Returns `{ decimals, symbol, balance, formattedBalance, refetchBalance, allowance, isSufficient, approve, approveData, isApprovePending, isApproveSuccess, refetchAllowance, isLoading, error }`. Honours `pipeline.mock.wallet.contract.<token>.decimals`, `…symbol`, `pipeline.mock.wallet.balance.<token>`, and approval mock keys.                                                                          |
| `useWithdrawalQueueAddresses`   | `@/wallet`    | Reads the `fromToken()` (→ PLUSD) and `intoToken()` (→ USDC) addresses from the WithdrawalQueue contract, exposed under domain-friendly aliases. Returns `{ plusd, usdc, isLoading, error }` with "fetch once" caching. Honours `pipeline.mock.wallet.contract.withdrawalQueue.plusd/usdc` named aliases and `pipeline.mock.wallet.contract.<address>.fromToken/intoToken` generic per-address fallbacks.                                                                                                                                                                       |
| `useWithdrawalVoucher`          | `@/api`       | React Query hook that fetches the withdrawal voucher (verifier signature) from `GET /v1/withdrawals/{request_id}/voucher?wallet=<addr>`. Returns `{ data, status, error, refetch }` where `status` is `"idle" \| "pending" \| "ready" \| "failed"`. Disabled when `requestId` is `undefined` or the wallet is disconnected. Same polling / retry semantics as `useDepositVoucher`. The EIP-712 domain for withdrawal vouchers differs from deposit, but the `signature` field shape is identical. Honours `pipeline.mock.api.GET./v1/withdrawals/<requestId>/voucher` mock key. |
| `useToast`                      | `@/lib/toast` | Imperative API for emitting transient toast notifications from page call sites. Returns `{ show, update, dismiss }`. `show({ id, tone, title, action?, icon?, durationMs? })` adds or upserts a toast (auto-dismisses in 5 s for non-pending tones). Must be called inside `<ToastProvider>`.                                                                                                                                                                                                                                                                                   |
| `useUnstake`                    | `@/wallet`    | Write hook for `redeem(shares, receiver, owner)` on the StakedPLUSD ERC-4626 vault; both `receiver` and `owner` default to the connected wallet. Returns `{ write, data: { hash, assets? }, isPending, isSuccess, error, reset }`. Honours `pipeline.mock.wallet.contract.stakedPlusd.unstake` for mock testing; `assets` is mock-path only.                                                                                                                                                                                                                                    |
| `useEvmWallet`                     | `@/wallet`    | Returns the connected wallet's address, connection state, chain id, and `connect`/`disconnect` actions. Backed by wagmi + Reown AppKit; honours `pipeline.mock.wallet.*` localStorage keys. `connect()` is gated by the shared chain-agnostic first-connection terms modal (see `useTermsAcknowledgement`) — AppKit is only opened after the user acknowledges the jurisdiction self-attestation. Mock-wallet paths bypass the gate as a dev affordance.                                                                                                                                                                                                                                                                                                                                                                                     |

## /test diagnostic route

The `/test` route (`packages/frontend/src/routes/test.tsx`) renders all wallet hooks and env values in one utilitarian page for manual QA and debugging. It uses `isMockKeyPresent` from `@/wallet` to show a `MOCKED` badge next to any field whose value is sourced from the `pipeline.mock.wallet.*` localStorage layer rather than a real RPC call. The page is intentionally not linked from `TopBar`; navigate to it directly at `http://localhost:5173/test`.

## How to add a row

1. Land the hook in code with the tests that cover its public contract.
2. Add a row above with the export name, the `@pipeline/...` (or `@/...`) import path, and a one-sentence description (what it returns and its primary side effect, if any).
3. Keep the table sorted alphabetically. If the hook is renamed, moved, or retires from "reused" status, update the row in the same commit.

name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # {{SETUP_STEP}} — replace with the project's runtime setup

      # {{INSTALL_STEP}} — replace with the project's dependency install

      - name: Unit tests
        run: {{TEST_COMMAND}}

  # Uncomment when integration tests are set up:
  # integration:
  #   runs-on: ubuntu-latest
  #   services:
  #     postgres:
  #       image: postgres:16
  #       env:
  #         POSTGRES_PASSWORD: postgres
  #         POSTGRES_DB: test
  #       ports:
  #         - 5432:5432
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #   steps:
  #     - uses: actions/checkout@v4
  #     # {{SETUP_STEP}}
  #     # {{INSTALL_STEP}}
  #     - name: Integration tests
  #       run: {{INTEGRATION_TEST_COMMAND}}
  #       env:
  #         DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test
